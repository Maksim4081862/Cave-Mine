from flask import Flask
from threading import Thread

app = Flask(__name__)

@app.route('/')
def home():
    return "I'm alive!"

def run_server():
    app.run(host='0.0.0.0', port=8080)

# Запустите это в main.py параллельно с ботом
Thread(target=run_server).start()
